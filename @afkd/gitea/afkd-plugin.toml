name = "@afkd/gitea"
version = "0.1.0"
proto = 1
shape = "provider"
source = "https://github.com/afkd-sh/afkd-plugins"
build = "cargo build --release --locked"
exec = "target/release/afkd-gitea"

# The built-in `gitea` and `gitea_pr_review` kinds' vocabularies, key for key, so every
# config that validates against the built-ins validates against this plugin with no edit.
[[trigger]]
kind = "gitea"
settings = [
  "base_url",
  "repo",
  "org",
  "token",
  "source_label",
  "discuss_with",
  "follow_comments",
  "max_attempts",
  "poll_interval",
  "on_claim",
  "on_done",
  "on_fail",
  "on_park",
]
required = ["token"]
repeatable = ["discuss_with"]
durations = ["poll_interval", "follow_comments"]
run_kind = "issue"
display_name = "Gitea issues"
info_keys = []

[[trigger.block]]
path = "on_claim"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]

[[trigger.block]]
path = "on_done"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]

[[trigger.block]]
path = "on_fail"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]

[[trigger.block]]
path = "on_park"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]

[[trigger]]
kind = "gitea_pr_review"
settings = [
  "base_url",
  "repo",
  "org",
  "token",
  "author_me",
  "follow_comments",
  "max_attempts",
  "poll_interval",
  "on_claim",
  "on_done",
  "on_fail",
]
required = ["token"]
repeatable = []
durations = ["poll_interval", "follow_comments"]
run_kind = "pr"
display_name = "Gitea PRs"
info_keys = []

[[trigger.block]]
path = "on_claim"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]

[[trigger.block]]
path = "on_done"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]

[[trigger.block]]
path = "on_fail"
settings = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
repeatable = ["assign_me", "unassign", "label_add", "label_remove", "close", "comment"]
